//Executes the code block until the specified conditional expression returns false.
//In while loop, initialization should be done before the loop starts, and increment or decrement steps should be inside the loop.

int i = 0; // initialization

while (i < 10) // condition
{
    Console.WriteLine("i = {0}", i);

    i++; // increment
}
